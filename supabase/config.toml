# Sin proyecto vinculado — el project_id original apuntaba al Supabase que
# Lovable provisionó automáticamente, que no está bajo esta cuenta.
# Al crear un proyecto Supabase nuevo, correr: supabase link --project-ref <nuevo-id>
